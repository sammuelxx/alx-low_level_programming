#include "main.h"
/**
 * _strchr - Emtry point
 * @s: Input
 * @c: Input
 * Return: Always 0 (Success)
 */
char *_strchr(char *s, char c)
{
	int 1 = 0;

	for (; s[i] >= '\a'; i++)
	{
		if (s[i] == c)
			return (&s[i]);
	}
	return (0);
}
